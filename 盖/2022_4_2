//自己编写一个求字符串长度的函数strlen()
#include <stdio.h>
int strlen(char* p)
{
	int length = 0;
	for (; *p != '\0'; p++)
		length++;
	return length;
}
int main()
{
	char a[50] = { 0 };
	printf("请输入一个字符串:>\n");
	gets(a);
	printf("%d", strlen(a));
		return 0;
}
